{
  "name": "@app/dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "mkdir -p dist && echo 'Dashboard placeholder' > dist/index.html",
    "preview": "vite preview"
  }
}
